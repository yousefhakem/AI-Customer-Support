# Written by `npx convex dev`. Runtime secrets are NOT read from here; set them on
# the deployment with `npx convex env set NAME value` (once for dev, once with --prod):
#   OPENAI_API_KEY, CLERK_JWT_ISSUER_DOMAIN, CLERK_SECRET_KEY, CLERK_WEBHOOK_SECRET,
#   AWS_REGION, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
CONVEX_DEPLOYMENT=
